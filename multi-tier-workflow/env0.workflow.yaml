environments:
  vpc:
    name: "VPC and Network"
    templateName: "vpc"

  db:
    name: "Database"
    templateName: "s3-bucket"
    requiresApproval: true
    needs:
      - vpc

  sg:
    name: "Security Group"
    templateName: "security-group"
    needs:
      - vpc

  ec2:
    name: "EC2"
    templateName: "ec2"
    needs:
      - db
      - sg

settings:
  environmentRemovalStrategy: destroy

