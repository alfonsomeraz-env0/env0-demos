version: 2

deploy:
  steps:
    helmDiff:
      before:
        - name: Configure kubectl
          run: |
            if [ -z "$CLUSTER_NAME" ] || [ "$CLUSTER_NAME" = "null" ] || \
               [ -z "$AWS_REGION" ]   || [ "$AWS_REGION" = "null" ]; then
              echo "Workflow outputs not available during plan phase — skipping kubeconfig setup"
              exit 0
            fi
            aws eks update-kubeconfig --region "$AWS_REGION" --name "$CLUSTER_NAME"

    helmUpgrade:
      before:
        - name: Configure kubectl
          run: |
            aws eks update-kubeconfig --region "$AWS_REGION" --name "$CLUSTER_NAME"
            echo "Cluster nodes:"
            kubectl get nodes

        - name: Helm lint
          run: |
            helm lint . -f values.yaml -f "$VALUES_FILE" && echo "Lint passed."

      after:
        - name: Verify rollout
          run: |
            RELEASE_NAME=${HELM_RELEASE_NAME:-nginx-demo}
            NAMESPACE=${HELM_NAMESPACE:-default}
            echo "Waiting for rollout..."
            kubectl rollout status deployment \
              -l app.kubernetes.io/instance="$RELEASE_NAME" \
              -n "$NAMESPACE" \
              --timeout=120s

            echo ""
            echo "--- Pods ---"
            kubectl get pods -l app.kubernetes.io/instance="$RELEASE_NAME" -n "$NAMESPACE"

            echo ""
            echo "--- Service ---"
            kubectl get svc -l app.kubernetes.io/instance="$RELEASE_NAME" -n "$NAMESPACE"

destroy:
  steps:
    helmUninstall:
      before:
        - name: Configure kubectl
          run: |
            aws eks update-kubeconfig --region "$AWS_REGION" --name "$CLUSTER_NAME"
